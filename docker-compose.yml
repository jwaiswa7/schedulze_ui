version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: jwaiswa/schedulze_ui:latest
    container_name: schedulze_ui
    ports:
      - "8080:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production

